__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/
venv/
build/
dist/
.DS_Store
*.env
credentials*

# Local AI assistant configuration. CLAUDE.md is local-only BY DESIGN: it may carry
# private corpus paths and numbers that must never reach a published surface (see the
# same-named rule inside it). Public engineering rules belong in CONTRIBUTING.md.
CLAUDE.md
.claude/
.codex/

# Local development notes: plans, issues, verification reports, research.
# Not part of the published product; see dev-notes/README.md for what goes where.
dev-notes/
